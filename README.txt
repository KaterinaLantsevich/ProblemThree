STEP 1: Open a terminal and navigate to the project directory.
        Run the following command to build and compile the project using Maven:

        mvn compile

STEP 2: Run the following command:

        mvn exec:java -Dexec.mainClass="org.example.Main"

        This command will execute the Main class, which contains the main method.
